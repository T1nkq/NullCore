<base:WpfUiWindow
    x:Class="Voidstrap.UI.Elements.Settings.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:Voidstrap.UI.Elements.Base"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dmodels="clr-namespace:Voidstrap.UI.ViewModels.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:Voidstrap.UI.Elements.Settings.Pages"
    xmlns:resources="clr-namespace:Voidstrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    xmlns:media="clr-namespace:System.Windows.Media.Animation;assembly=PresentationCore"
    Title="{x:Static resources:Strings.Menu_Title}"
    Width="1071"
    Height="690"
    MinWidth="1071"
    MinHeight="690"
    d:DataContext="{d:DesignInstance dmodels:MainWindowViewModel, IsDesignTimeCreatable=True}"
    AllowsTransparency="False"
    WindowStyle="None"
    Background="#FF070A0E"
    Closed="WpfUiWindow_Closed"
    Closing="WpfUiWindow_Closing"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Disable"
    WindowStartupLocation="CenterScreen"
    Loaded="MainWindow_Loaded"
    UseLayoutRounding="True"
    SnapsToDevicePixels="True"
    mc:Ignorable="d">

    <base:WpfUiWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <BitmapImage x:Key="AppIconImage"
                     UriSource="pack://application:,,,/Voidstrap.ico" />
        <SolidColorBrush x:Key="NullCoreShellBrush" Color="#F0090D12" />
        <SolidColorBrush x:Key="NullCorePanelBrush" Color="#E812171E" />
        <SolidColorBrush x:Key="NullCorePanelAltBrush" Color="#D91A222B" />
        <SolidColorBrush x:Key="NullCoreStrokeBrush" Color="#336EE7C8" />
        <SolidColorBrush x:Key="NullCoreStrokeMutedBrush" Color="#243A4654" />
        <SolidColorBrush x:Key="NullCoreAccentBrush" Color="#FF6EE7C8" />
        <SolidColorBrush x:Key="NullCoreCyanBrush" Color="#FF7CB8FF" />
        <SolidColorBrush x:Key="NullCoreWarningBrush" Color="#FFFFC857" />
        <Style x:Key="NullCoreHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="NullCoreMetaTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
            <Setter Property="FontSize" Value="12" />
        </Style>
        <media:CubicEase x:Key="SmoothEaseOut" EasingMode="EaseOut" />
        <media:QuinticEase x:Key="UltraSmoothEaseOut" EasingMode="EaseOut" />
        <LinearGradientBrush x:Key="LightningBrush"
                     StartPoint="0,0"
                     EndPoint="1,0">
            <GradientStop Color="#00000000" Offset="0.0"/>
            <GradientStop Color="#AAFFFFFF" Offset="0.45"/>
            <GradientStop Color="#FFFFFFFF" Offset="0.5"/>
            <GradientStop Color="#AAFFFFFF" Offset="0.55"/>
            <GradientStop Color="#00000000" Offset="1.0"/>
        </LinearGradientBrush>

        <Storyboard x:Key="LightningStoryboard"
                    RepeatBehavior="Forever">
            <DoubleAnimation
    Storyboard.TargetName="LightningTranslate"
    Storyboard.TargetProperty="X"
    From="-900"
    To="1800"
    Duration="0:0:1.2"
    EasingFunction="{StaticResource SmoothEaseOut}" />


            <DoubleAnimation
                Storyboard.TargetName="LightningLayer"
                Storyboard.TargetProperty="Opacity"
                From="0.3"
                To="0.9"
                Duration="0:0:0.06"
                AutoReverse="True"
                RepeatBehavior="Forever"/>
        </Storyboard>
        <Storyboard x:Key="IntroStoryboard">

            <!-- Scale X -->
            <DoubleAnimationUsingKeyFrames
        Storyboard.TargetName="IntroScaleTransform"
        Storyboard.TargetProperty="ScaleX">
                <EasingDoubleKeyFrame Value="0.96" KeyTime="0:0:0" />
                <EasingDoubleKeyFrame Value="1.015" KeyTime="0:0:0.55"
                              EasingFunction="{StaticResource UltraSmoothEaseOut}" />
                <EasingDoubleKeyFrame Value="1.0" KeyTime="0:0:0.9"
                              EasingFunction="{StaticResource SmoothEaseOut}" />
            </DoubleAnimationUsingKeyFrames>

            <!-- Scale Y -->
            <DoubleAnimationUsingKeyFrames
        Storyboard.TargetName="IntroScaleTransform"
        Storyboard.TargetProperty="ScaleY">
                <EasingDoubleKeyFrame Value="0.96" KeyTime="0:0:0" />
                <EasingDoubleKeyFrame Value="1.015" KeyTime="0:0:0.55"
                              EasingFunction="{StaticResource UltraSmoothEaseOut}" />
                <EasingDoubleKeyFrame Value="1.0" KeyTime="0:0:0.9"
                              EasingFunction="{StaticResource SmoothEaseOut}" />
            </DoubleAnimationUsingKeyFrames>

            <!-- Overlay fade -->
            <DoubleAnimation
        Storyboard.TargetName="IntroOverlay"
        Storyboard.TargetProperty="Opacity"
        From="1.0"
        To="0.0"
        BeginTime="0:0:0.6"
        Duration="0:0:0.8"
        EasingFunction="{StaticResource SmoothEaseOut}"
        FillBehavior="Stop" />

            <!-- Lightning fade -->
            <DoubleAnimation
        Storyboard.TargetName="LightningLayer"
        Storyboard.TargetProperty="Opacity"
        To="0"
        BeginTime="0:0:0.6"
        Duration="0:0:0.8"
        EasingFunction="{StaticResource SmoothEaseOut}"
        FillBehavior="Stop" />

        </Storyboard>
    </base:WpfUiWindow.Resources>

    <Grid Background="#FF070A0E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:Name="BackgroundLayer"
      Grid.RowSpan="3"
      Panel.ZIndex="-10"
      ClipToBounds="True">

            <MediaElement x:Name="BackgroundMedia"
                  Stretch="UniformToFill"
                  Opacity="0"
                  IsHitTestVisible="False"
                  LoadedBehavior="Manual"
                  UnloadedBehavior="Stop"
                  Visibility="Collapsed" />

            <Image x:Name="BackgroundImage"
           Stretch="UniformToFill"
           Opacity="0"
           RenderOptions.BitmapScalingMode="HighQuality"
           IsHitTestVisible="False"
           Visibility="Collapsed"/>

            <Border x:Name="GradientLayer">
                <Border.Background>
                    <LinearGradientBrush x:Name="MainBackgroundBrush"
                                 StartPoint="0,0"
                                 EndPoint="1,1">
                        <LinearGradientBrush.RelativeTransform>
                            <TransformGroup>
                                <RotateTransform x:Name="BackgroundGradientRotate"
                                         CenterX="0.5"
                                         CenterY="0.5"/>
                                <TranslateTransform x:Name="BackgroundGradientTranslate"/>
                            </TransformGroup>
                        </LinearGradientBrush.RelativeTransform>

                        <GradientStop Offset="0.0" Color="#FF070A0E" />
                        <GradientStop Offset="0.45" Color="#FF090E14" />
                        <GradientStop Offset="1.0" Color="#FF05070A" />
                    </LinearGradientBrush>
                </Border.Background>
            </Border>
        </Grid>

        <Canvas x:Name="SnowCanvas"
                Grid.RowSpan="3"
                IsHitTestVisible="False"
                Visibility="Collapsed"
                Panel.ZIndex="0" />


        <Grid x:Name="IntroOverlay"
      Grid.RowSpan="3"
      Panel.ZIndex="999"
      Background="#CC0A0A0A"
      Opacity="1"
      RenderTransformOrigin="0.5,0.5">

            <Grid.RenderTransform>
                <ScaleTransform x:Name="IntroScaleTransform"
                                ScaleX="1"
                                ScaleY="1" />
            </Grid.RenderTransform>

            <Grid x:Name="LightningLayer"
                  IsHitTestVisible="False"
                  Opacity="0.8">

                <Rectangle Height="100"
                           Margin="0,0,0,60"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Center"
                           Fill="{StaticResource LightningBrush}">
                    <Rectangle.RenderTransform>
                        <TranslateTransform x:Name="LightningTranslate"/>
                    </Rectangle.RenderTransform>
                    <Rectangle.Effect>
                        <BlurEffect Radius="35"/>
                    </Rectangle.Effect>
                </Rectangle>

            </Grid>

            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Orientation="Vertical">
                <Image Width="120"
                       Height="120"
                       Source="{StaticResource AppIconImage}"
                       Stretch="Uniform"
                       Margin="0,0,0,12" />

                <TextBlock Text="NullCore"
                           FontSize="28"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Foreground="White"
                           Opacity="0.95" />

                <TextBlock Text="t1nkq &amp; DeniskaaaXD"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Foreground="White"
                           Opacity="0.7"
                           Margin="0,4,0,0" />
            </StackPanel>
        </Grid>

        <ui:Snackbar x:Name="AlreadyRunningSnackbar"
                     Title="{x:Static resources:Strings.Menu_AlreadyRunning_Title}"
                     Grid.RowSpan="10"
                     Margin="200,0,200,20"
                     Panel.ZIndex="10"
                     Icon="Info20"
                     Message="{x:Static resources:Strings.Menu_AlreadyRunning_Caption}"
                     Timeout="10000">
            <ui:Snackbar.Background>
                <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
                    <GradientStop Offset="0.00" Color="{DynamicResource WindowBackgroundColorPrimary}" />
                    <GradientStop Offset="0.70" Color="{DynamicResource WindowBackgroundColorSecondary}" />
                    <GradientStop Offset="1.00" Color="{DynamicResource WindowBackgroundColorThird}" />
                </LinearGradientBrush>
            </ui:Snackbar.Background>
        </ui:Snackbar>

        <ui:Snackbar x:Name="SettingsSavedSnackbar"
             Title="{x:Static resources:Strings.Menu_SettingsSaved_Title}"
             Grid.RowSpan="10"
             Margin="200,0,200,20"
             Panel.ZIndex="9"
             Appearance="Dark"
             Icon="CheckboxChecked24"
             Message="{x:Static resources:Strings.Menu_SettingsSaved_Message}"
             Timeout="2100">
            <ui:Snackbar.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Offset="0.0" Color="{DynamicResource WindowBackgroundColorPrimary}" />
                    <GradientStop Offset="0.35" Color="{DynamicResource WindowBackgroundColorSecondary}" />
                    <GradientStop Offset="0.75" Color="{DynamicResource WindowBackgroundColorThird}" />
                    <GradientStop Offset="1.0" Color="{DynamicResource WindowBackgroundColorPrimary}" />
                </LinearGradientBrush>
            </ui:Snackbar.Background>
        </ui:Snackbar>
        
        <ui:Snackbar x:Name="SettingsSavedLaunchSnackbar"
             Title="NullCore Saved And Launched Roblox!"
             Grid.RowSpan="10"
             Margin="150,0,150,20"
             Panel.ZIndex="10"
             Appearance="Dark"
             Icon="CheckboxChecked24"
             Message="Changes have been saved."
             Timeout="2000">
            <ui:Snackbar.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Offset="0.0" Color="{DynamicResource WindowBackgroundColorPrimary}" />
                    <GradientStop Offset="0.35" Color="{DynamicResource WindowBackgroundColorSecondary}" />
                    <GradientStop Offset="0.75" Color="{DynamicResource WindowBackgroundColorThird}" />
                    <GradientStop Offset="1.0" Color="{DynamicResource WindowBackgroundColorPrimary}" />
                </LinearGradientBrush>
            </ui:Snackbar.Background>
        </ui:Snackbar>

        <ui:TitleBar Padding="8"
                     x:Name="RootTitleBar"
                     Grid.Row="0"
                     ForceShutdown="False"
                     MinimizeToTray="False"
                     UseSnapLayout="True"
                     Title="{x:Static resources:Strings.Menu_Title}"
                     Icon="pack://application:,,,/Voidstrap.ico" />

        <Grid x:Name="RootGrid"
              MouseMove="RootGrid_MouseMove"
              MouseLeave="RootGrid_MouseLeave"
              Grid.Row="1"
              Margin="8,12,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.ColumnSpan="2"
                Margin="0,0,8,0"
                Background="{StaticResource NullCoreShellBrush}"
                BorderBrush="{StaticResource NullCoreStrokeMutedBrush}"
                BorderThickness="1"
                CornerRadius="10" />

            <ui:NavigationFluent
                x:Name="RootNavigation"
                Grid.Row="1"
                Grid.Column="0"
                Width="222"
                Margin="8,6,6,10"
                Padding="8,8,8,10"
                Background="{StaticResource NullCorePanelBrush}"
                BorderBrush="{StaticResource NullCoreStrokeMutedBrush}"
                BorderThickness="1"
                Frame="{Binding ElementName=RootFrame}"
                SelectedPageIndex="0">
                <ui:NavigationFluent.Items>
                    <ui:NavigationItem Content="Core" IsEnabled="False" Opacity="0.6" />
                    <ui:NavigationItem Content="Dashboard" Icon="DesktopPulse24" Tag="DesktopPulse32" PageType="{x:Type pages:InstanceDashboardPage}" />
                    <ui:NavigationItem Content="{x:Static resources:Strings.Menu_Integrations_Title}" Icon="Add12" Tag="Add16" PageType="{x:Type pages:IntegrationsPage}" />
                    <ui:NavigationItem Content="Deployment" Icon="PlaySettings20" Tag="Play20" PageType="{x:Type pages:BehaviourPage}" />
                    <ui:NavigationSeparator />
                    <ui:NavigationItem Content="Configuration" IsEnabled="False" Opacity="0.6" />
                    <ui:NavigationItem Content="FastFlag Settings" Icon="Settings28" Tag="SettingsChat24" PageType="{x:Type pages:FastFlagsPage}" />
                    <ui:NavigationItem x:Name="FastFlagEditorNavItem" Content="FastFlag Editor" Icon="Flag28" Tag="Flag20" PageType="{x:Type pages:FastFlagEditorPage}" Visibility="{Binding IsFastFlagEditorVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <ui:NavigationItem Content="Global" Icon="GlobeLocation20" Tag="Globe20" PageType="{x:Type pages:GBSEditorPage}" />
                    <ui:NavigationItem Content="{x:Static resources:Strings.Menu_Mods_Title}" Icon="WrenchScrewdriver20" Tag="Wrench24" PageType="{x:Type pages:ModsPage}" />
                    <ui:NavigationSeparator />
                    <ui:NavigationItem IsEnabled="False" Opacity="0.6" />
                    <ui:NavigationItem Content="News" Icon="News16" Tag="News20" Margin="0,-40,0,20" PageType="{x:Type pages:NewsPage}" />
                </ui:NavigationFluent.Items>

                <ui:NavigationFluent.Footer>
                    <ui:NavigationItem Content="{x:Static resources:Strings.Common_Shortcuts}" Icon="Apps32" Tag="Apps24" PageType="{x:Type pages:ShortcutsPage}" />
                    <ui:NavigationItem Content="Settings" Icon="Settings28" Tag="Settings20" PageType="{x:Type pages:ChannelPage}" />
                    <ui:NavigationItem Content="{x:Static resources:Strings.Menu_About_Title}" Icon="QuestionCircle32" Tag="QuestionCircle48" Command="{Binding OpenAboutCommand, Mode=OneTime}" />
                </ui:NavigationFluent.Footer>
            </ui:NavigationFluent>

            <Grid Grid.Row="0" Grid.RowSpan="3" Grid.Column="1" Margin="0,6,16,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border
                    Grid.RowSpan="2"
                    Background="{StaticResource NullCorePanelBrush}"
                    BorderBrush="{StaticResource NullCoreStrokeMutedBrush}"
                    BorderThickness="1"
                    CornerRadius="10" />

                <Grid Grid.Row="0" Margin="14,12,14,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Border
                        Width="42"
                        Height="42"
                        Margin="0,0,12,0"
                        Background="{StaticResource NullCorePanelAltBrush}"
                        BorderBrush="{StaticResource NullCoreStrokeBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                        <Image
                            Width="24"
                            Height="24"
                            Source="{StaticResource AppIconImage}"
                            RenderOptions.BitmapScalingMode="HighQuality" />
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="NullCore" FontSize="20" Style="{StaticResource NullCoreHeaderTextStyle}" />
                            <Border
                                Margin="10,1,0,0"
                                Padding="8,3"
                                VerticalAlignment="Center"
                                Background="#1811161D"
                                BorderBrush="{Binding NetworkStatusBrush}"
                                BorderThickness="1"
                                CornerRadius="5">
                                <StackPanel Orientation="Horizontal">
                                    <Grid Width="12" Height="12" Margin="0,0,6,0" VerticalAlignment="Center">
                                        <Ellipse Fill="{Binding NetworkStatusBrush}" Opacity="0.2">
                                            <Ellipse.Triggers>
                                                <EventTrigger RoutedEvent="Loaded">
                                                    <BeginStoryboard>
                                                        <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.15" To="0.55" Duration="0:0:1.2" />
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </Ellipse.Triggers>
                                        </Ellipse>
                                        <Ellipse Width="6" Height="6" Fill="{Binding NetworkStatusBrush}" />
                                    </Grid>
                                    <TextBlock
                                        Text="{Binding NetworkStatusText}"
                                        FontSize="11"
                                        FontWeight="SemiBold"
                                        Foreground="{Binding NetworkStatusBrush}" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <ui:SymbolIcon
                                x:Name="BreadcrumbIcon"
                                Symbol="Add12"
                                FontSize="16"
                                Margin="0,1,6,0"
                                VerticalAlignment="Center"
                                Foreground="{StaticResource NullCoreCyanBrush}" />
                            <ui:Breadcrumb Navigation="{Binding ElementName=RootNavigation}" />
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <ui:TextBox x:Name="GlobalSearchBox"
                            Width="240"
                            Margin="0,0,8,0"
                            HorizontalAlignment="Right"
                            PlaceholderText="Search..."
                            TextChanged="GlobalSearchBox_TextChanged"
                            ClearButtonEnabled="True"
                            Icon="Search24"
                            Visibility="{Binding ShowSearchBox, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </StackPanel>

                    <ui:Button
                        Grid.Column="3"
                        MinWidth="128"
                        Margin="0"
                        Padding="8,5"
                        HorizontalAlignment="Right"
                        Appearance="Secondary"
                        Background="#FF111820"
                        BorderBrush="{Binding UpdateStatusBrush}"
                        Command="{Binding CheckUpdateCommand}">
                        <StackPanel Orientation="Horizontal">
                            <Border
                                Width="22"
                                Height="22"
                                Margin="0,0,8,0"
                                Background="#08000000"
                                BorderBrush="{Binding UpdateStatusBrush}"
                                BorderThickness="1"
                                CornerRadius="4">
                                <ui:SymbolIcon
                                    FontSize="13"
                                    Symbol="{Binding UpdateStatusIcon}"
                                    Foreground="{Binding UpdateStatusBrush}" />
                            </Border>
                            <TextBlock
                                VerticalAlignment="Center"
                                FontSize="12"
                                FontWeight="SemiBold"
                                Text="{Binding UpdateStatusText}"
                                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                        </StackPanel>
                    </ui:Button>

                </Grid>
                <Frame x:Name="RootFrame" Grid.Row="1" Margin="14,0,14,14" NavigationUIVisibility="Hidden"/>
            </Grid>
        </Grid>

        <Border Grid.Row="2"
                VerticalAlignment="Bottom"
                Height="58"
                Margin="8,0,8,8"
                Background="{StaticResource NullCoreShellBrush}"
                BorderBrush="{StaticResource NullCoreStrokeMutedBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="16,0">

            <StatusBar x:Name="RootStatusBar"
                       Padding="14,10"
                       BorderThickness="0,1,0,0">
                <StatusBar.Background>
                    <SolidColorBrush Color="{ui:ThemeResource ApplicationBackgroundColor}" Opacity="0" />
                </StatusBar.Background>

                <StatusBar.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </StatusBar.ItemsPanel>

                <StatusBarItem Grid.Column="0" Margin="0,0,4,0">
                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                        <ui:ToggleSwitch Background="{DynamicResource SystemAccentBrush}"
                                         Foreground="{DynamicResource SystemAccentBrush}"
                                         IsChecked="{Binding TestModeEnabled, Mode=TwoWay}" />
                        <TextBlock Padding="10,0,0,0"
                                   VerticalAlignment="Center"
                                   FontSize="13"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Text="{x:Static resources:Strings.Menu_TestMode}" />

                    </StackPanel>
                </StatusBarItem>

                <StatusBarItem Grid.Column="2" Padding="0,0,8,0">
                    <ui:Button x:Name="InstallLaunchButton"
                               MinWidth="126"
                               HorizontalAlignment="Center"
                               Appearance="Primary"
                               Background="{StaticResource NullCoreAccentBrush}"
                               Command="{Binding SaveAndLaunchSettingsCommand, Mode=OneWay}"
                               Content="{x:Static resources:Strings.Menu_SaveAndLaunch}" />
                </StatusBarItem>

                <StatusBarItem Grid.Column="3" Padding="0,0,9,0">
                    <ui:Button MinWidth="92"
                               Padding="14,0"
                               Appearance="Primary"
                               Background="{StaticResource NullCoreCyanBrush}"
                               Click="Button_Click_1"
                               Command="{Binding SaveSettingsCommand, Mode=OneWay}"
                               Content="{x:Static resources:Strings.Menu_Save}" />
                </StatusBarItem>

                <StatusBarItem Grid.Column="4" Padding="0,0,8,0">
                    <ui:Button MinWidth="92"
                               Padding="14,0"
                               HorizontalAlignment="Center"
                               Appearance="Secondary"
                               Command="{Binding CloseWindowCommand, Mode=OneTime}"
                               Click="Button_Click_2"
                               Content="{x:Static resources:Strings.Common_Close}" />
                </StatusBarItem>
            </StatusBar>
        </Border>
    </Grid>
</base:WpfUiWindow>
